#!/usr/bin/python

import os

VariantDir('build/semisupervised', 'semisupervised')
VariantDir('build/scripts', 'scripts')
VariantDir('build/pdis', 'pdis')

SConscript('build/semisupervised/SConscript')
SConscript('build/pdis/presentation1/SConscript')


